{
  "name": "@kairo/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "kairo-mcp": "./src/bin.ts"
  },
  "scripts": {
    "dev": "tsx src/bin.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "test": "vitest run",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kairo/core": "workspace:*",
    "@kairo/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
